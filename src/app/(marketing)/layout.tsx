/** Paginas publicas: titulos con el serif de marca. */
export default function MarketingLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <div className="brand-serif">{children}</div>;
}
